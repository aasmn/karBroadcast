import React, { Component } from 'react';
class Main extends Component {
    render() {
        return (
            <div className="chart-title">{this.props.title}
            </div>
        );
    }
}

export default Main;
